# No third-party dependencies — this app uses only the Python standard library.
# This file exists solely so Railway's build system detects a Python project
# and installs the Python runtime specified in .python-version.
